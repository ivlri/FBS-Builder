# RL FBS Builder — Архитектура системы
## Roadmap

```
Этап 1 (MVP):     Одна стена без проёмов
Этап 2:           Domain randomization (разные длины стен)
Этап 3:           Одна стена с заблокированными зонами      <- ЗАВЕРШЕН
Этап 4:           Context Builder + несколько связанных стен <- ТЕКУЩИЙ
Этап 5:           Planner с эвристикой порядка обхода
Этап 6:           Экономическая оптимизация (цены блоков)
Этап 7:           Production с confidence scoring
```

# Этап 4

## Цель

Реализовать Context Builder для перевязки смежных стен (chess-pattern bonding).

## Изменения в архитектуре

**Было:** `FBSBuilder.reset()` создаёт пустую сетку `np.zeros()`

**Стало:** `ContextBuilder.build_grid()` создаёт сетку с предзаполненными ограничениями → передаёт в `FBSBuilder`

## Правила перевязки торцов

Входные данные: `walls[]`, `current_idx`
- `completed_wall` = walls[current_idx - 1] (если есть)
- `next_wall` = walls[current_idx + 1] (если есть)

### Паттерн ограничений

Ограничения чередуются по 600мм-слоям (layer = row // 2):

| Layer | Left (from completed) | Right (towards next) |
|-------|----------------------|---------------------|
| 0     | BLOCKED              | free                |
| 1     | free                 | BLOCKED             |
| 2     | BLOCKED              | free                |
| 3     | free                 | BLOCKED             |

**Логика в коде** (`contextbuilder.py:44,54`):
```python
# Left end: block when layer % 2 == 0
if block % 2 != 1:  # layers 0, 2, 4...
    grid[layer, :cells] = 1

# Right end: block when layer % 2 == 1
if block % 2 == 1:  # layers 1, 3, 5...
    grid[layer, -cells:] = 1
```

### Пример: 3 стены подряд

```
Wall 0 (первая):    [.........|FREE]  — нет completed, нечего блокировать слева
Wall 1 (средняя):   [BLOCKED..|..BLOCKED]  — ограничения с обеих сторон (чередуются)
Wall 2 (последняя): [BLOCKED..|FREE]  — нет next, нечего блокировать справа
```

### Ширина ограничения

Ширина блокируемой зоны = `wall.weight` (толщина примыкающей стены):
- 300mm wall → 15 cells @ 20mm grid
- 200mm wall → 10 cells @ 20mm grid

## Статус реализации

- [x] `ContextBuilder.build_grid()` — создание сетки с ограничениями
- [x] `ContextBuilder._apply_end_restrictions()` — логика перевязки
- [x] `ModelRunner.run()` — интеграция с context_builder
- [x] `context_test.py` — тесты на 3 стенах


# Переосмысмысление planer и ContextBuilder
Что должно происходить реально
```
Перед запуском RL:
    Planner строит граф:
        W1 — W2 — W3
        Для узлов определяет:
            есть ли пересечение
            ширина пересечения
    Теперь ContextBuilder должен:
        для КАЖДОЙ стены заранее создать:
            left_reserved_mask
            right_reserved_mask
            Даже для W1.
```
# Возможный вариант 1


## Архитектура становится такой

Для стены Wi:

1. Planner генерирует возможные corner profiles (2 варианта перевязки)

2. Для каждого профиля:
```py
    context = ContextBuilder.build(wall, profile, built_neighbors)
    result = RL.predict(context)
    score = evaluate(result)
```

3. Выбирается лучший профиль (может даже есть смысл раскладывать по 2 стены за раз, что бы в итоге получать лучшее качество)
    Возможные метрики: 
    1. % FBS
    2. Количество монолита
    3. Количество коротких блоков
    5. Количество стыков
    6. Общую стоимость (задать всем обьектам стоимость)

    score = 5 * fbs_ratio - 2 * monolith_volume - 1 * seam_count

## Когда нужна сложная оптимизация

Если:

3-х слойная нестандартная перевязка
    Т-стыки
    4 стены в узле
    разные высоты
    разные веса
Тогда можно сделать:
    Beam Search (k=2 или 3)
    Dynamic Programming по графу стен

Сейчас вопрос в том что возможно нужно пересмотреть архитектуру. Rl выпускать в глобальное поле смысла нет, так как умрет policy. Как обойтись детерминированным алгоритмом для определения какие ограничения передать агенту в конкретный момент, я не понимаю. Ввод такого ограничения может привести к тому что глобальное качество раскладки блоков будет очень не оптимальным, просто потому что в какой-то момент было принято решение, которое просто перекрыло возможность сделать оптимальное решение. Как будто бы все сводится к тому что на месте детерминируемого planer должен быть второй RL агент, который уже будет обучаться на основе работы первого. То есть система обучения такая. RL-агент_№1 получит на вход от planer набор стен (я могу предоставить много конфигураций для обучения) в порядке обхода -> замет агент начнет для выбранной стены задавать один из двух типов шахматной перевязки блоков в торце -> затем RL-агент №1 запустить инференс уже обученного RLагента №2 -> раскладка RLвгнета №2 будет формировать награду для RL-агент_№1 на основе метрик (    Возможные метрики: 
    1. % FBS
    2. Количество монолита
    3. Количество коротких блоков
    5. Количество стыков
    6. Общую стоимость)) -> цель RL-агента №1 научиться стратегии выбора перевязки блоков в стене. 


Либо рассмотреть задачу как задачу оптимизации xmax​i∑​Li​(xi​)+(i,j)∈E∑​Iij​(xi​,xj​) - то к чему я склоняюсь но пока не понимаю как применить. Я можно сказать знаю теорию оптимизации, но никогда не применял ее в задаче и тем более самостоятельно. Если верно поставить задачу то можно будет использовать готовые солверы для решения за приемлимое время 

Li​(xi​) - Это результат запуска RL №2 на стене i при выбранной перевязке. Так фиксируется влияние выбора перевязки на качество кладки.
Iij​(xi​,xj​) - Это penalty за несовместимость перевязок. Например: несовпадение вертикальных швов, накопление коротких блоков на стыке, конструктивные ограничения
Global discrete optimization (graph)
        ↓
Local learned constructive policy (RL)